extends Container
class_name AudioBankLineUI

@export var DeleteTriggerButton : BaseButton
@export var PlayRandomButton : BaseButton
@export var TriggerNameEdit : LineEdit
@export var SettingsButton : BaseButton
@export var ResourceList : Container

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
    pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
    pass
